# cpu_opt Engine

This directory is reserved for the optimized CPU engine.

It may use OpenMP, SIMD, cache-friendly layouts, and algorithmic restructuring.
Every supported feature must pass parity tests against `cpu_nep3` before it is
treated as production-ready.
